name: Hola Mundo
on:
  push:
  workflow_dispatch:
jobs:
  hola:
    runs-on: ubuntu-latest
    steps:
    - name: saludar
      run: echo "Hola Mundo"
    - name: Descargar Codigo
      uses: actions/checkou@v4
    - name: En que carpeta estoy
      run: pwd